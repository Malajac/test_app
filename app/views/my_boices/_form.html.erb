<%= form_for(@my_boice) do |f| %>
  <% if @my_boice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@my_boice.errors.count, "error") %> prohibited this my_boice from being saved:</h2>

      <ul>
      <% @my_boice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
